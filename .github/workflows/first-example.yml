name: My First Workflow

on: push

jobs: 
    first_job:
        runs-on: ubuntu-latest
        steps: 
        - name: Welcome Message
        run: echo "my forst github actions job"

        - name: List Files
        run: ls

        - name: Read File
        run Cat.Readme